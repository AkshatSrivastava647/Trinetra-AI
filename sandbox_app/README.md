# sandbox_app

The deliberately vulnerable demo application -- this is Trinetra's detection & patch target.

Plan: a small Flask app modeled loosely on a logistics/reporting backend, with an
intentional SQL injection vulnerability (raw string-concatenated query) for Trinetra
to find and fix.

Status: not yet built.
